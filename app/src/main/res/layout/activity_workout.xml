<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".WorkoutActivity"
    android:fitsSystemWindows="true">

    <!-- Editable workout title w pencil icon -->
    <ImageView
        android:id="@+id/ivEditTitle"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:contentDescription="@string/edit"
        android:src="@android:drawable/ic_menu_edit"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/etWorkoutTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:autofillHints=""
        android:hint="@string/workout_title_hint"
        android:textSize="22sp"
        android:textStyle="bold"
        android:inputType="textCapWords"
        android:padding="8dp"
        app:layout_constraintStart_toEndOf="@id/ivEditTitle"
        app:layout_constraintTop_toTopOf="@id/ivEditTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/ivEditTitle"
        android:layout_marginEnd="16dp" />

    <!-- Add exercise -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAddExercise"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/add_exercise"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginTop="12dp"
        app:cornerRadius="12dp"
        style="@style/Widget.MaterialComponents.Button"
        app:layout_constraintTop_toBottomOf="@id/etWorkoutTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvExercises"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="16dp"
        android:clipToPadding="false"
        tools:listitem="@layout/item_exercise"
        app:layout_constraintTop_toBottomOf="@id/btnAddExercise"
        app:layout_constraintBottom_toTopOf="@id/btnEndWorkout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- End workout (bottom) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnEndWorkout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="End Workout"
        android:layout_margin="10dp"
        app:cornerRadius="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>